{
    "encodec_48_12":    {   "install_cmd": ["pip install encodec"],
                            "import": ["from encodec import EncodecModel"],
                            "model_init": ["model = EncodecModel.encodec_model_48khz()",
                                           "model.set_target_bandwidth(12.0)"],
                            "sample_rate": 48000,
                            "in_shape": 3,
                            "embed_fwd": ["encoded_frames = model.encode(x)",
                                          "y = torch.cat([encoded[0] for encoded in encoded_frames], dim=-1)"]
                        }
}