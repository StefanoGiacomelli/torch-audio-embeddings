{
    "epanns":   {   "install_cmd": ["pip install epanns-inference",
                                    "pip install numpy==2.0"],
                    "import": ["from epanns_inference import models"],
                    "model_init": ["model = models.Cnn14_pruned(pre_trained=True)"],
                    "sample_rate": 32000,
                    "in_shape": 2,
                    "embed_fwd": ["y = model(x)",
                                  "y = y['embedding']"]
                }
}