{
    "wavlm_base_plus":  {   "install_cmd": ["pip install torchaudio"],
                            "import": ["import torchaudio"],
                            "model_init": ["model = torchaudio.pipelines.WAVLM_BASE_PLUS.get_model()"],
                            "sample_rate": 16000,
                            "in_shape": 2,
                            "embed_fwd": ["y = model.extract_features(x)",
                                          "y = y[-2][0]"]
                        }
}